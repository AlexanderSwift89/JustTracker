# JustTracker — тест-план (тестировщик)

## 1. Стратегия

| Уровень | Что проверяем | Инструмент | Когда |
|---------|---------------|------------|-------|
| Unit (JVM) | Geo, LocationFilter, effectiveSpeed, ElevationCalculator, TrackStatsCalculator, IncrementalStats, ActivityClassifier, GpxWriter; (1.1) WikipediaRef, PoiFactory, OverpassQl/GeoCell, PoiProximity, PoiSummary, OverpassParser, WikiSummaryParser; (1.2) SpeedProfile/TrackPath, SpeedColorScale, Track.recordingTimeMs; (JustTracker) ProviderPolicy, AppLanguage, LatLonBox/ofTile, MapSourceResolver, RegionTransitions, DownloadManager reason → RegionError, RegionCatalogParser + валидность `assets/maps/regions.json` | JUnit 4 (+ `org.json` для парсеров), `./gradlew :app:testDebugUnitTest` | каждый коммит |
| Static | Lint (MissingPermission, Security, i18n) | `./gradlew :app:lintDebug` | каждый коммит |
| Integration | Room DAO + TrackRepository (finish/классификация) | androidTest, Room in-memory | перед релизом |
| Ручное функциональное | Все US из PRD | Эмулятор API 34 (`adb emu geo fix`) + реальное устройство | перед релизом |
| Ручное нефункциональное | Батарея, поведение при выключенном экране, kill процесса | Реальное устройство, 1 ч записи | перед релизом |

Текущее покрытие unit-тестами: **107 тестов** (36 MVP + 29 «Интересное рядом» + 16 время записи/скорость/курсор + 26 JustTracker: провайдер геолокации, язык, офлайн-карты), все зелёные (см. `android/app/src/test`).

## 2. Тестовые данные

Синтетические треки генерируются в тестах функцией `syntheticTrack(speedMps, seconds, segment, jitter)`:
- пешком 1.4 м/с, бег 3.0 м/с, велосипед 6.0 м/с, авто 15 м/с;
- «город с остановками» — 80 % точек 4 м/с + 20 % 20 м/с → должен классифицироваться как CAR;
- шум: ±20 % по скорости, ±0.2 м по координатам, единичный выброс 40 м/с.

Для ручного тестирования на эмуляторе — скрипт подачи точек (PowerShell, **обязательно инвариантная культура — иначе `55,75` с запятой ломает `geo fix`**):
```powershell
$adb="D:\Android_sdk\platform-tools\adb.exe"; $lat=55.7558; $lon=37.6173
for($i=0;$i -lt 60;$i++){ $lat += 0.000027; $lon += 0.000015
  $s="$($lon.ToString('F6',[cultureinfo]::InvariantCulture)) $($lat.ToString('F6',[cultureinfo]::InvariantCulture)) 150"
  & $adb emu geo fix $s.Split(' ') | Out-Null; Start-Sleep 1 }
```
Шаг 0.000027° широты ≈ 3 м/с (бег). Для «пешком» — 0.000012, для «авто» — 0.00012.

Особенность эмулятора: `Location.speed` ≈ 0 всегда — именно поэтому введена эвристика `effectiveSpeed` (§3.3 системной спецификации).

## 3. Тест-кейсы (ручные)

Обозначения: ✅ — пройден на эмуляторе API 34 в ходе разработки; ⬜ — требует прогона на реальном устройстве.

| ID | US | Шаги | Ожидаемый результат | Статус |
|----|----|------|---------------------|--------|
| TC-01 | US-14 | Первый запуск | Онбординг с объяснением; «Продолжить» → системные диалоги location → notifications → экран «Запись» | ✅ |
| TC-02 | US-14 | Отказать в геолокации | Панель «Нужен доступ к геолокации» с кнопкой «Разрешить»; после второго отказа — «Открыть настройки» | ⬜ |
| TC-03 | US-01 | Нажать «Старт» при выключенной геолокации | Диалог с кнопкой «Открыть настройки»; баннер сверху карты | ⬜ |
| TC-04 | US-01 | Нажать «Старт» | Уведомление «Идёт запись трека», кнопки Пауза/Стоп, бейдж на вкладке, маркер на карте | ✅ |
| TC-05 | US-05 | Двигаться 60 с | Линия трека растёт, карта следует за маркером | ✅ |
| TC-06 | US-05 | Сдвинуть карту пальцем | Автоследование выключается, появляется FAB «Центрировать»; нажатие включает следование | ⬜ |
| TC-07 | US-06 | Наблюдать панель | Мгновенная скорость, дистанция, средняя и макс. обновляются ≤ 3 с; **время записи** тикает каждую секунду (в т.ч. без GPS-фиксов), время движения — второстепенной строкой | ✅ |
| TC-08 | US-06 | Остановить подачу GPS на 15 с | Индикатор «Поиск GPS…», скорость «—» | ✅ (виден при восстановлении) |
| TC-09 | US-02 | Пауза → движение → Продолжить | Во время паузы точки не пишутся, время движения стоит, время записи продолжает идти; после продолжения линия не соединяет точку паузы с новой (новый сегмент) | ✅ (пауза), ⬜ (сегмент после продолжения) |
| TC-10 | US-03 | Стоп → Завершить | Открывается экран деталей; трек в истории | ✅ |
| TC-11 | US-03 | Стоп сразу после старта (< 2 точек) | Снекбар «Слишком мало данных», трек не сохранён | ✅ |
| TC-12 | US-04 | Во время записи выключить экран на 10 мин | Точки за все 10 мин присутствуют | ⬜ |
| TC-13 | US-04 | Во время записи `adb shell am kill` / переустановка APK | При открытии — диалог «Незавершённая запись»; «Продолжить» возобновляет запись новым сегментом с сохранением накопленных данных | ✅ |
| TC-14 | US-04 | Смахнуть приложение из Recents во время записи | Уведомление и запись сохраняются (`stopWithTask=false`) | ⬜ |
| TC-15 | US-07 | Открыть «История» | Список карточек: иконка типа, название, дата, дистанция, время записи, средняя; второй строкой — «В движении … · макс. …» | ✅ |
| TC-16 | US-07 | Пустая история | Пустое состояние с подсказкой | ✅ |
| TC-17 | US-08 | Открыть трек | Карта с полным треком (fit bounds), линия окрашена по скорости, легенда, старт (зелёный), финиш (красный), плитки: дистанция, время записи, время движения, средняя, макс., набор, сброс, темп (пешие), точек, паузы (если были), начало | ✅ |
| TC-18 | US-09 | Записать трек со скоростью бега | Тип определён как «Бег», линия оранжевая | ✅ |
| TC-19 | US-09 | Изменить тип вручную | Bottom sheet, выбранный тип отмечен; после смены — иконка/цвет обновлены; повторная автоклассификация не применяется | ⬜ |
| TC-20 | US-10 | Переименовать (из деталей и долгим нажатием в истории) | Имя обновляется в списке | ⬜ |
| TC-21 | US-10 | Удалить | Диалог подтверждения; трек исчезает из истории и статистики | ⬜ |
| TC-22 | US-11 | Экспорт GPX | Системный диалог «Поделиться» с файлом `<name>_<id>.gpx`; файл — валидный GPX 1.1 с trkseg на сегмент | ✅ |
| TC-23 | US-12 | Открыть «Статистика» | Итоги, неделя, месяц, самый длинный, лучшая средняя, по типам | ✅ |
| TC-24 | US-13 | Переключить единицы на имперские | Все значения в mi/ft/mph | ⬜ |
| TC-25 | US-13 | Тёмная тема | Карта инвертирована, контраст панелей достаточный | ⬜ |
| TC-26 | US-13 | Порог точности 10 м + плохой сигнал | Точки с accuracy > 10 м отбрасываются | ⬜ |
| TC-27 | NFR-01 | 1 ч записи на реальном устройстве | Расход батареи ≤ 8 % | ⬜ |
| TC-28 | — | Поворот экрана во время записи | Состояние сохраняется, запись не прерывается | ⬜ |
| TC-29 | — | Отозвать разрешение геолокации во время записи (через настройки) | Запись не падает; после возврата — панель разрешений | ⬜ |
| TC-30 | — | Локаль ru | Все строки переведены, плюралы «1 трек / 2 трека / 5 треков» | ⬜ |
| TC-31 | US-16 | Открыть «Запись» с сетью, положение в центре города (`geo fix 37.6173 55.7558`) | В течение ~5 с появляются оранжевые пины (Большой и Малый театры…); в logcat `Overpass: 192 places`, показаны 80 ближайших, один запрос на ячейку | ✅ |
| TC-32 | US-16 | Тап по пину | Bottom sheet: название (локализованное для ru), категория, расстояние, спиннер → текст лида; кнопки «Прочитать вслух», «Википедия» | ✅ |
| TC-33 | US-16 | Пройти ~600 м (сменить ячейку) | Новый запрос не раньше чем через 15 с после предыдущего; старые пины остаются до ответа; за пределами 1,5 км пины исчезают | ⬜ |
| TC-34 | US-16 | Режим полёта, открыть «Запись» | Пинов нет, ошибок нет; после включения сети и смены ячейки (или через 60 с и перезахода) пины появляются | ⬜ |
| TC-35 | US-16 | Карточка трека с ≥ 2 точками | Пины в полосе 400 м вдоль трека (в центре Москвы: 126 получено → 60 ближайших к линии показаны) | ✅ |
| TC-36 | US-17 | «Прочитать вслух» в карточке | Речь на языке статьи; кнопка → «Стоп»; повторный тап останавливает; закрытие карточки останавливает; музыка приглушается на время речи | ✅ эмулятор: кнопка «Стоп», `dumpsys audio` — фокус `GAIN_TRANSIENT_MAY_DUCK` / `USAGE_ASSISTANCE_NAVIGATION_GUIDANCE`, Google TTS синтезирует `ru-RU`; слышимость — ⬜ на устройстве |
| TC-37 | US-17 | Настройки → выключить «Показывать интересные места» | Пины исчезают на обоих экранах, переключатель авто-озвучки заблокирован, в logcat нет запросов Overpass | ✅ |
| TC-38 | US-17 | Авто-озвучка вкл., старт записи, `geo fix` с шагом к объекту ближе 150 м, экран выключен | Объект объявлен один раз; повторный проход мимо него в том же треке — тишина; новый трек — объявляется снова | ✅ эмулятор, экран включён (2 объекта объявлены подряд при проходе мимо Большого театра); экран выключен / повторный проход — ⬜ |
| TC-39 | US-17 | Авто-озвучка: два объекта в радиусе | Объявляется ближайший, затем второй на следующем обновлении (QUEUE_ADD — не перебивает) | ⬜ |
| TC-40 | Security | Overpass-ответ с тегом `wikipedia=evil.com/x:Title` (unit) | Объект отброшен, запрос к чужому хосту невозможен | ✅ (unit) |
| TC-41 | Security | Перехват трафика (mitmproxy без установленного user-CA) | Запросы к Overpass/Wikipedia не проходят (system CA only), приложение не падает | ⬜ |
| TC-42 | US-16 | Устройство с локалью de, объект только с `name` и `wikipedia=en:…` | Имя по умолчанию, статья en, TTS en | ⬜ |
| TC-43 | US-15 | Запись: 30 с медленно (1 м/с), затем 30 с быстро (6 м/с) | Первая часть линии синяя/зелёная, вторая — оранжевая/красная; легенда показывает макс. ≈ 22 km/h; плитка «Макс.» на панели совпадает с легендой | ✅ |
| TC-44 | US-15 | Тап по медленному и по быстрому участку линии на экране записи | Карточка «Скорость на участке» с соответствующей скоростью, расстоянием от старта и временем с начала записи; кольцо на тапнутой точке; карточка исчезает через 6 с или по тапу; follow не отключается | ✅ |
| TC-47 | US-08 | Детали: перетащить слайдер, нажать ◀ ▶, тапнуть по линии | Кольцо курсора едет по линии; подпись «время от старта · %» и значения (скорость, дистанция, время суток, высота) меняются синхронно; тап по линии переносит слайдер; 0 % — старт, 100 % — финиш | ⬜ (логика покрыта unit-тестами; эмулятор в сессии падал — SIGSEGV swiftshader) |
| TC-45 | US-06 | Уведомление во время записи | Хронометр времени записи тикает в шторке без обновлений от GPS; текст «дистанция · скорость» | ✅ |
| TC-46 | US-08 | Трек, записанный в 1.1 (до обновления) | Время записи = finishedAt − startedAt, линия окрашена по скорости из сохранённых точек — без миграции | ⬜ |
| **JustTracker 1.0.0** | | | | |
| TC-48 | US-18 | Первый запуск на устройстве с локалью `en` | Splash → экран «Choose your language», предвыбран English; «Русский» + Continue → экран разрешений на русском; дальнейший UI на русском | ✅ (эмулятор API 34) |
| TC-49 | US-18 | Первый запуск с локалью `ru` | Предвыбран «Русский» | ⬜ |
| TC-50 | US-18 | Настройки → Язык → English | Интерфейс мгновенно на английском без перезапуска; текущая вкладка сохраняется; идущая запись не прерывается | ✅ |
| TC-51 | US-18 | После TC-50: kill процесса, перезагрузка устройства | Язык сохранён (API 26–32 — через AppLocalesMetadataHolderService; 33+ — системно); на Android 13+ пункт «Язык приложения» в системных настройках показывает тот же выбор | ⬜ (API 34 после kill — ✅) |
| TC-52 | US-18 | Запись на русском языке приложения при системной локали `en` | Автоимя трека, уведомление записи, карточка POI (заголовок статьи `ru:` при наличии), fallback TTS — на русском | ⬜ (автоимя ✅) |
| TC-53 | US-19 | Офлайн-карты → «Мальта» → Скачать, Wi-Fi есть | Диалог с размером 6,7 МБ → «В очереди» → «Загрузка… N %» → «Проверка файла…» → «Готов · 6,7 МБ»; в Настройках «1 регион · 6,7 МБ» | ✅ |
| TC-54 | US-19 | «Только по Wi-Fi» включён, Wi-Fi выключен, мобильная сеть есть | Статус «Ожидание Wi-Fi»; после включения Wi-Fi загрузка стартует сама | ⬜ |
| TC-55 | US-19 | Отмена во время загрузки | Строка возвращается в каталог, `.part`-файла нет в папке `maps` | ⬜ |
| TC-56 | US-19 | Свободного места меньше размера региона + 200 МБ | Snackbar «Недостаточно свободного места», загрузка не создаётся | ⬜ |
| TC-57 | US-19 | Авиарежим, позиция внутри скачанного региона, запись | Карта региона отрисовывается (улицы, подписи), линия трека поверх; за границей региона — пусто/кэш, ошибок нет | ✅ |
| TC-58 | US-19 | Авиарежим, обзорный зум < 8 | Тайлы из кэша или пустые; после включения сети подгружаются онлайн | ⬜ |
| TC-59 | US-19 | Удалить регион (подтверждение) | Файл удалён, «Регионы не скачаны», карта области снова требует сеть | ⬜ |
| TC-60 | US-19 | Убить процесс во время загрузки | Загрузка продолжается (уведомление системы); после запуска статус актуален, регион становится «Готов» | ⬜ |
| TC-61 | US-19 | Подменить `.part` на мусор перед завершением / повреждённый файл | Статус «Файл не является картой», файл удалён, «Повторить» доступно | ⬜ |
| TC-62 | US-20 | Импорт `.map` через системный диалог | Регион в «Импортированных файлах» со статусом «Готов», используется на карте | ⬜ |
| TC-63 | US-20 | Импорт не-карты (txt/png) | Snackbar «Файл не является картой», ничего не сохранено | ⬜ |
| TC-64 | US-20 | Скопировать `.map` в `Android/data/com.justtracker.app/files/maps/` вручную, перезапустить | Регион подхвачен как импортированный | ⬜ |
| TC-65 | US-21 | Эмулятор AOSP без Google APIs (или Huawei без GMS): онбординг → запись 30 с → стоп → детали → экспорт | Всё работает, нет диалогов о сервисах Google; фиксы приходят (GPS/fused AOSP) | ⬜ (на образе с GMS — ✅) |
| TC-66 | US-21 | `./gradlew :app:dependencies` | Нет артефактов `com.google.android.gms` | ✅ |
| TC-67 | M3 | Планшет / развёрнутая ширина при не компактной высоте | Навигация — rail слева, экраны корректны; телефон в landscape — bottom bar (компактная высота) | ✅ (телефон landscape) |
| TC-68 | M3 | Android 14+ с включённой анимацией predictive back: жест назад из деталей трека и из карточки POI | Превью предыдущего экрана, без крэшей | ⬜ |
| TC-69 | M3 | Холодный старт | Splash с иконкой на зелёном фоне держится до загрузки настроек; онбординг/главный экран без мерцания | ✅ |
| TC-70 | M3 | Тема → Тёмная при светлой системе | Панели тёмные, карта инвертирована, иконки статус-бара светлые | ✅ |
| TC-71 | M3 / a11y | TalkBack по Настройкам и Офлайн-картам | Строки со switch читаются как один переключатель с состоянием; у всех IconButton есть описание | ⬜ |
| TC-72 | Nav | Настройки → смена языка (пересоздание activity) → вкладка «Запись» | Вкладка открывается (регрессия D-07) | ✅ |

## 4. Найденные и исправленные дефекты (в ходе разработки)

| # | Дефект | Причина | Исправление |
|---|--------|---------|-------------|
| D-01 | Крэш на онбординге | `painterResource` не поддерживает adaptive-icon | Векторная иконка в круге |
| D-02 | Мгновенная скорость и время движения = 0 на эмуляторе | Приёмник отдаёт `speed ≈ 0` при реальном движении | `effectiveSpeed` + время движения по смещению |
| D-03 | Карта рисовалась поверх карточек статистики | `MapView` не клипуется в `Column` | `Modifier.clipToBounds()` |
| D-04 | Автоимя трека содержало время завершения, а не старта | При завершении без живой сессии брался `now` | Используется `track.startedAt` |
| D-05 | Имя GPX-файла с подряд идущими `_` | Regex без квантификатора `+` | `[^A-Za-z0-9._-]+` |
| D-06 (1.1) | Нет пина у Большого театра в центре Москвы | `out center 60` отдаёт первые 60 объектов по id, а не ближайшие | Запрос 200 + ранжирование по расстоянию на клиенте (`PoiProximity.nearest/nearestToLine`) |
| D-07 (JT) | После пересоздания activity (смена языка) вкладка «Запись» не открывалась | `navigate(start) { popUpTo(start, saveState) }` не срабатывал на восстановленном стеке | Клик по вкладке сначала пробует `popBackStack(route, saveState = true)`, и только если её нет в стеке — `navigate` |
| D-08 (JT) | `topOnly()` не применялся к `MaterialTheme.shapes.extraLarge` | Расширение объявлено для `RoundedCornerShape`, а темa отдаёт `CornerBasedShape` | Receiver — `CornerBasedShape` |

## 5. Открытые наблюдения

- **OBS-02 (1.1).** Публичный Overpass отвечает `504` под нагрузкой (поймано при smoke-тесте) — обрабатывается backoff'ом 60 с, метки не обновляются. Если жалобы в отзывах — добавить второй инстанс (`overpass.kumi.systems`) как fallback.
- **OBS-03 (1.1).** В центре мегаполиса в полосе 400 м вокруг 500-метрового трека Overpass возвращает > 120 объектов; после ограничения до 60 ближайших карта всё ещё плотная. Кандидат на 1.2: кластеризация или фильтр по категориям.
- **OBS-04 (1.1).** На устройстве с локалью `en` название берётся из `name:en`, а статья — из `wikipedia` (часто `ru:`), поэтому имя и описание могут быть на разных языках. Это ожидаемо по спецификации (§3.7); улучшение — запрашивать `langlinks` для языка устройства.

- **OBS-05 (JT).** На экранах высокой плотности подписи офлайн-карты крупные: mapsforge рендерит тайл 256 px, osmdroid масштабирует его по DPI (то же, что с онлайн-тайлами). Кандидат: рендер тайлов размером `256·density` (копия `MapsForgeTileSource` с настраиваемым размером тайла).
- **OBS-06 (JT).** В тёмной теме офлайн-карта инвертируется вместе с онлайн-тайлами: вода становится коричневой. Кандидат: тёмная render-тема Mapsforge вместо `INVERT_COLORS` для офлайн-тайлов.
- **OBS-07 (JT).** В `malta.map` встречается подпись с префиксом языка («deTor am Leuchtturm») независимо от выбранного языка — особенность данных файла, не приложения.
- **OBS-08 (JT).** DownloadManager на эмуляторе с Google APIs иногда пишет `PAUSED_WAITING_FOR_NETWORK` при включённом Wi-Fi на несколько секунд после постановки — статус «Ожидание сети» мигает; на реальном устройстве проверить.

- **OBS-01.** На медленном эмуляторе (software GPU) дважды наблюдался «фантомный» клик через ~8 с после `adb input tap` (один раз запустил запись, один раз завершил). В трёх последующих попытках не воспроизвёлся; в коде источник не найден (все команды сервису логируются в debug). Требуется проверка на реальном устройстве; если воспроизведётся — добавить debounce на кнопки Старт/Стоп.

## 6. Регрессионный чек-лист перед релизом

1. `./gradlew :app:testDebugUnitTest :app:lintDebug` — зелёные.
2. TC-01, 04, 05, 07, 09, 10, 12, 13, 17, 18, 22, 31, 32, 36, 37, 38, 43, 44, 45, 47, **48, 50, 51, 53, 57, 59, 62, 65, 70, 72** на реальном устройстве (минимум Android 10 и Android 14+).
3. Проверка release-сборки (R8): запуск, запись, экспорт, **офлайн-регион в авиарежиме** — на release APK/AAB, подписанном release-ключом.
4. Проверка уведомления на Android 13+ без разрешения на уведомления — запись продолжается.
5. TC-65 на образе **без Google APIs** (AOSP) — обязательный пункт для RuStore.
6. Скриншоты в `store/rustore/screenshots/` соответствуют текущему UI (пересъёмка при изменении экранов).

## 7. Матрица устройств

| API | Версия | Устройство | Приоритет |
|-----|--------|-----------|-----------|
| 26 | 8.0 | эмулятор | низкий (minSdk sanity) |
| 29 | 10 | реальное бюджетное (Xiaomi/Samsung A) | высокий — агрессивный battery killer |
| 31 | 12 | реальное | средний |
| 33 | 13 | реальное | высокий — POST_NOTIFICATIONS |
| 34 | 14 | эмулятор Pixel 7 Pro, Google APIs (использован для JT: TC-48, 50, 53, 57, 67, 69, 70, 72) | высокий — FGS types |
| 34 | 14 | эмулятор **AOSP без Google APIs** (`system-images;android-34;default;x86_64`) | высокий — US-21, TC-65 |
| 29–31 | 10–12 | реальное без GMS (Huawei/Honor) или эмулятор API 30 | высокий — per-app locale через AppCompat (TC-51), LocationManager без fused |
| 35–36 | 15–16 | реальное Pixel | высокий — целевая версия |
